AB数组都是16x16大小的int二维数组,int占4个字节
1. 每一行所占用的大小为16 * 4 = 64字节, 所以对Aptr来说,它要初始调整到第i行的第一个元素的位置,也就是定位到A + 64i的地址, 也就是汇编代码中2,3行所做的操作
2. 要将Bptr初始定位到第0行第k列的位置,也就是B + 4 * k,它对应汇编代码第4行的操作
3. 由于Bptr按行步进,每次增加一行即64字节,并且这样的步进会执行16次,最终会停留在B[N][k], 即B + 16 * 64 + k * 4这个地址,也就是Bptr + 1024, 所以便翻译为汇编代码中第5行的操作